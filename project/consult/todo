--1/28/2018 linkbox jump correctlymon
--do not jump if use click a same link


--load all banner resource  

--adjust size on mobile programmatically 
    hide top menu when window too narrow ,change to a button ,when press, menu slide from right
--achieve a object pool use to smooth animation (not smooth on mobile now)
ok========================================
//feature---------------------------------
*client*
--use pure javascript and css -- no template, no new syntax
--send basic html with data ,describe needed html info
--build html entire on client side
  + page are defined via seperate js file , 
--dynamic include other js as view component
  + 1/18/2018--do not include js if already include   --1/19/2018

*server*
--use pure nodejs, no other package needed
--support restful routine  
--seperate routine to different files

*data*
-- use mongoose connect app and database
-- use mlab free mongodb service
*deploy*
-- deploy to heroku use github
//function -------------------------------
-- 1/16/2018-- display todo list when press logo--1/16/2018
--1/16/2018-- jump to different page via a linkMap
            --!!not use a linkMap, define it direct in menu data--1/17/2018
//layout----------------------------------
--use one code add foot and head simutaniously 
--hideable strip that display brief information
X--change img use css**not very flexible,css change txt not possible

//language--------------------------------
--language button , switch between two language ,use the database

//effect----------------------------------
--box shadow except top
--pull down menu animation
--rolling banner
  keep rolling when mouse cursor out  the banner area, 
  stop immediatelly when mouse over
  hide page navigator when mouse cursor out the banner 
  !!stop rolling when user move mouse or click window or lost focus(include change to other tab or change to other window) 

//fix bug
+ 1/18/2018 fix a pulldown menu bug
 -- rewrite pulldown menu response method
 --1/28/2018--fix bug -- menu don't disappear when mouse move quickly

//log
--1/28/2018 sidemenu link jump correctly
--1/28/2018 change detailbox style  on welcome page 
--1/28/2018 --adddetail function bund same function in deferent file
+ 1/19/2018--construct introduce overview page
  -- 1/20/2018  --write sideMenu component -- 1/21/2018/1
  -- 1/23/2018  --write overview content ,X--refresh a area without load whole page--X
  -- 1/24/2018  --use a uniform detailBox,
  -- 1/25/2018  X--generate menuPath at serverside--X
                --build link chain. e.g. about > introduce > overview
               --write searchMenuPath function, one detailBox hook deferent detail.
  -- 1/26/2018  --get big text through getContentText   
  -- 1/27/2018   ok--define head string indb(advisory...)
                ok--add banner text in db
                ok--%3E for "?" , give possibility to  set translate "no" 
                ok--download a local base64 encode/decode software
                ok--entire offline development
                ok--import/export all data between local database to remote database
                ok--adjust overview page layout

