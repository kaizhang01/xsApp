-- 1/18/2018 fix a pulldown menu bug
            rewrite pulldown menu response method

-- dynamic include component ,do not include  if already include                 

--get content language text use Post Method

--adjust size on mobile programmatically 
    hide top menu when window too narrow ,change to a button ,when press, menu slide from right
--achieve a object pool use to smooth animation (not smooth on mobile now)
ok========================================
//feature---------------------------------
*client*
--use pure javascript and css -- no template, no new syntax
--send basic html with data describe needed html info
  and then build html entire on client side
  dynamic include other js as view component

*server*
--use pure nodejs, no other package needed
--support restful routine  
--seperate routine to different files

*data*
-- use mongoose connect app and database
-- use mlab free mongodb service
*deploy*
-- deploy to heroku use github
//function -------------------------------
-- 1/16/2018-- display todo list when press logo--1/16/2018
--1/16/2018-- jump to different page via a linkMap
                !!not use a linkMap, define it direct in menu data--1/17/2018
//layout----------------------------------
--add foot
--hideable strip that display brief information
--change img use css**not very flexible,css change txt not possible

//language--------------------------------
--language button , switch between two language ,use the database

//effect----------------------------------
--box shadow except top
--pull down menu animation
--keep rolling when mouse cursor out  the banner area, stop immediatelly when mouse over
  hide page navigator when mouse cursor out the banner 
  !!stop rolling when user move mouse or click window or lost focus(include change to other tab or change to other window) 